services:
  - type: web
    name: flask-crypto
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app -c gunicorn_config.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
